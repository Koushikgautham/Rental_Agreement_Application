import 'package:flutter/material.dart';

class TenantsPage extends StatelessWidget {
  const TenantsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tenants'),
      ),
      body: const Center(
        child: Text('Tenants Page - Coming Soon'),
      ),
    );
  }
}