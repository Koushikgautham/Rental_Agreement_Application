import 'package:flutter/material.dart';

class CreateAgreementPage extends StatelessWidget {
  const CreateAgreementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Agreement'),
      ),
      body: const Center(
        child: Text('Create Agreement Page - Coming Soon'),
      ),
    );
  }
}