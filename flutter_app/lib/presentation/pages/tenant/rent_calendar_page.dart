import 'package:flutter/material.dart';

class RentCalendarPage extends StatelessWidget {
  const RentCalendarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rent Calendar')),
      body: const Center(child: Text('Rent Calendar - Coming Soon')),
    );
  }
}