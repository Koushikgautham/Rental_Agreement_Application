import 'package:flutter/material.dart';

class CreateMaintenancePage extends StatelessWidget {
  const CreateMaintenancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Maintenance Request')),
      body: const Center(child: Text('Create Maintenance - Coming Soon')),
    );
  }
}